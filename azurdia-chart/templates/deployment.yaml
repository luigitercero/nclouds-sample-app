apiVersion: v1
kind: Namespace
metadata:
  name: azurdia-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "azurdia-chart.fullname" . }}
  labels:
    app: {{ template "azurdia-chart.name" . }}
  namespace: azurdia-dev
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "azurdia-chart.name" . }}
  template:
    metadata:
      labels:
        app: {{ template "azurdia-chart.name" . }}
    spec:
      containers:
      - name: {{ .Chart.Name }}
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        env:
        - name: APPLICATION
          value: {{ template "azurdia-chart.name" . }}
        ports:
        - containerPort: {{ .Values.service.port }}
        resources:
          limits:
            memory: {{ .Values.resources.limits.memory }}